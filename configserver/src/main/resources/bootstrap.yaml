
server:
  port: 8889    # 컨피그 서버가 수신 대기하는 포트
spring:
  cloud:
    config:
      server:
        # encrypt:
        #   enabled: false
        git:
          uri: file://C:/myhome/03_Study/13_SpringCloud/config-repo
